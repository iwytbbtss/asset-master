import Body from './Body';

const Auth = async () => {
  return (
    <main className='flex flex-col justify-center items-center w-full h-screen'>
      <Body />
    </main>
  );
};

export default Auth;
